---
phase: 01-corpus-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/eval_corpus/scan.py
  - src/eval_corpus/manifest.py
  - src/eval_corpus/cli.py
  - tests/test_scan.py
  - tests/test_manifest.py
autonomous: true
requirements:
  - CORP-02
user_setup: []
must_haves:
  truths:
    - 递归枚举语料根下文件，默认不跟随 symlink
    - 输出 JSON 含 `schema_version`，默认路径 `./corpus_manifest.json`（由 `--manifest-out` 控制，与 D-07 一致）
    - 人类可读摘要输出在 stderr（D-06）
  artifacts:
    - `corpus_manifest.json` 可由 CLI 在临时目录中生成且通过 JSON 解析
  key_links:
    - 忽略 `~$*` 与 `.DS_Store`（D-11）
---

<objective>
实现语料递归扫描、扩展名白名单与忽略规则；生成清单 JSON（`schema_version`、文件级元数据）；将人类可读摘要打印到 stderr，满足 CORP-02 与 D-05～D-11。
</objective>

<execution_context>
@C:\Users\一枚blink\.cursor\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/01-corpus-baseline/01-CONTEXT.md
@src/eval_corpus/config.py
@src/eval_corpus/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扫描与过滤</name>
  <read_first>
    - src/eval_corpus/config.py
    - .planning/phases/01-corpus-baseline/01-CONTEXT.md（D-08～D-11）
  </read_first>
  <files>src/eval_corpus/scan.py, tests/test_scan.py</files>
  <behavior>
    - `iter_corpus_files(root: Path) -> Iterator[Path]`：`rglob("*")` 或 `os.walk` 且 `follow_symlinks=False`
    - 默认扩展名集合含：`.pdf`, `.docx`, `.xlsx`, `.pptx`, `.txt`, `.md`, `.png`, `.jpg`, `.jpeg`, `.tif`, `.tiff`（小写比较）
    - 跳过文件名匹配 `~$*`、名为 `.DS_Store` 的文件
    - 可选 CLI：`--extra-ext` 多次追加扩展名（含或不含点均可，实现需规范化）
    - **可读性（CORP-02）：** 枚举时若某路径 `stat`/`open` 失败（权限、锁、断链），将该文件记入 `errors[]` 或跳过并在 manifest 层汇总 `unreadable_count`；stderr 摘要中提示存在不可读项（与「运行前校验可读」一致，不要求强行读取二进制全文）
  </behavior>
  <action>
    实现 `scan.py` 导出 `DEFAULT_EXTENSIONS: frozenset[str]` 与 `iter_corpus_files(root, extensions, extra_exts=())`。
    在 `tmp_path` 下构造目录树：含允许扩展名、拒绝扩展名、`~$foo.docx`、`.DS_Store`、symlink（Windows 若需 skip 则条件跳过测试）。
    增加不可读文件用例：在 Unix 使用 `chmod 0` 文件或在 Windows 使用无法打开的路径模拟（若 CI 受限则 mock `Path.stat` 抛 `PermissionError`），断言扫描器记录错误或跳过且不崩溃。
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_scan.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/eval_corpus/scan.py` 存在且含 `DEFAULT_EXTENSIONS`
    - `tests/test_scan.py` 至少断言：允许文件数量、忽略 `~$` 文件
    - `tests/test_scan.py` 含针对 `PermissionError` 或不可读路径的测试函数名包含 `unreadable` 或 `permission`
  </acceptance_criteria>
  <done>扫描规则与测试覆盖 D-08～D-11 的核心行为。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 清单 JSON 与 stderr 摘要</name>
  <read_first>
    - src/eval_corpus/scan.py
    - .planning/phases/01-corpus-baseline/01-CONTEXT.md（D-05～D-07）
  </read_first>
  <files>src/eval_corpus/manifest.py, src/eval_corpus/cli.py, tests/test_manifest.py</files>
  <behavior>
    - 子命令 `manifest`：`--manifest-out` 默认 `corpus_manifest.json`；`--root` 继承 Plan 01
    - JSON 顶层：`schema_version: "1.0"`, `root`, `generated_at` (ISO8601), `files: [{ "path", "size_bytes", "suffix" }]`
    - stderr：一行或多行人类可读：文件数、总字节、根路径
  </behavior>
  <action>
    `manifest.py`：`build_manifest(root: Path, files: list[Path]) -> dict`。CLI 调 `resolve_corpus_root` → 扫描 → 写 JSON（UTF-8）→ `typer.secho(summary, err=True)`。
    `tests/test_manifest.py`：临时目录写入 2 个文件，运行构建函数或 invoke Typer `CliRunner`，断言 JSON 键与 stderr 含文件数。
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_manifest.py tests/test_scan.py</automated>
  </verify>
  <acceptance_criteria>
    - 生成的 JSON 文件含 `"schema_version": "1.0"`
    - `src/eval_corpus/cli.py` 包含字符串 `manifest`（子命令注册）
    - `<automated>` 已通过（覆盖 Typer help 行为）
  </acceptance_criteria>
  <done>CORP-02 清单与摘要行为可测。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地文件系统 | 仅枚举用户指定根下的文件元数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation | symlink | mitigate | 默认 `follow_symlinks=False`（D-10） |
| T-01-05 | Information disclosure | 清单 JSON | accept | 路径为相对根或绝对由选项控制；文档说明勿提交含敏感路径的工件 |

</threat_model>

<verification>
`pytest -q tests/test_scan.py tests/test_manifest.py` 全绿。
</verification>

<success_criteria>
- CORP-02 扫描、清单、校验可读在自动化测试中体现
- D-05～D-07、D-11 在实现与测试中可追溯

</success_criteria>

<output>
完成后创建 `.planning/phases/01-corpus-baseline/01-02-SUMMARY.md`
</output>
