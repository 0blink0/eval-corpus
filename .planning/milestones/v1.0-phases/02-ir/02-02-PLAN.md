---
phase: 02-ir
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/eval_corpus/chunker.py
  - tests/test_chunker_core.py
autonomous: true
requirements:
  - CHUNK-02
must_haves:
  truths:
    - "分块器按 300-1000 字目标输出文本 chunk，并支持句界优先 + 硬切回退。"
    - "table 块保持原子，不参与重叠；文本块重叠严格按 D-21 公式执行。"
    - "重叠异常场景（前块过短）按 D-23 缩短而非报错。"
  artifacts:
    - path: "src/eval_corpus/chunker.py"
      provides: "统一分块核心 API 与结构优先 + 重叠算法实现"
    - path: "tests/test_chunker_core.py"
      provides: "长度、重叠、table 原子、跨页元数据行为测试"
  key_links:
    - from: "src/eval_corpus/chunker.py"
      to: "src/eval_corpus/ir_models.py"
      via: "list[ParsedBlock] -> list[Chunk]"
      pattern: "def .*chunk.*\\(.*ParsedBlock"
    - from: "tests/test_chunker_core.py"
      to: "src/eval_corpus/chunker.py"
      via: "规则断言"
      pattern: "overlap|table|max_chars|min_chars"
---

<objective>
实现统一分块算法主体，覆盖长度约束、结构优先、句界优先切分与重叠规则，直接满足 CHUNK-02。

Purpose: 将 D-20~D-23 的语义变为可重复执行的纯 Python 逻辑，为 CLI 与后续多工具适配提供统一引擎。
Output: chunker 核心实现与手工夹具单元测试。
</objective>

<context>
@.planning/phases/02-ir/02-CONTEXT.md
@.planning/phases/02-ir/02-RESEARCH.md
@.planning/phases/02-ir/02-01-PLAN.md
@src/eval_corpus/ir_models.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现结构优先分块与句界/硬切策略</name>
  <read_first>.planning/phases/02-ir/02-CONTEXT.md, src/eval_corpus/ir_models.py</read_first>
  <files>src/eval_corpus/chunker.py</files>
  <action>实现纯 Python 分块 API（落实 D-27 接口形态）：输入 list[ParsedBlock] + 配置，输出 list[Chunk]。先做结构分段：table 作为原子块直接保留（D-20）；文本块按句界优先切分，不满足 max_chars 时执行硬切回退；长度目标遵循 min_chars/max_chars（默认 300/1000，配置可覆写）。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_core.py::test_chunking_rules -x</automated>
  </verify>
  <acceptance_criteria>文本 chunk 在可行场景满足长度区间；table 不被拆分到多个 chunk；API 不依赖任何工具私有字段。</acceptance_criteria>
  <done>chunker.py 已提供结构优先分块主流程，table 原子与句界/硬切策略由自动化用例稳定验证。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现文本重叠公式与边界容错</name>
  <read_first>.planning/phases/02-ir/02-CONTEXT.md, src/eval_corpus/chunker.py</read_first>
  <files>src/eval_corpus/chunker.py, tests/test_chunker_core.py</files>
  <action>仅对非 table 文本 chunk 应用重叠（D-20）；按 D-21 实现 overlap_chars=ceil(overlap_ratio*len(current_chunk_text))，使用 Python str 字符索引复制前块尾部作为后块前缀（D-22）；当前块需要的重叠超过前块可提供长度时按 D-23 截短并保持流程成功，可选标记 overlap_truncated。补充 Unicode 与短前块测试覆盖。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_core.py::test_overlap_formula_and_truncation -x</automated>
  </verify>
  <acceptance_criteria>重叠率落点与公式一致；table chunk 重叠恒为 0；前块过短不会抛错且结果可解释；Unicode 字符不发生断裂异常。</acceptance_criteria>
  <done>D-21~D-23 的重叠与截短逻辑落地完成，含 Unicode 与短前块边界测试并可重复通过。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParsedBlock 输入 -> 分块核心 | 输入结构合法但内容可能极端（超长文本、异常分页） |
| 算法输出 -> 后续评测 | 若重叠/长度规则偏差，会污染公平对比基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `chunker.py` 规则实现 | mitigate | 用单元测试钉死 D-20~D-23 的关键公式和边界行为 |
| T-02-05 | DoS | 超长文本切分 | mitigate | 采用线性遍历与受控切分，避免指数级回溯或递归爆栈 |
| T-02-06 | Information Disclosure | 调试日志 | accept | Phase 2 仅本地离线语料，不引入外发日志通道 |
</threat_model>

<verification>
针对 CHUNK-02 执行核心规则测试：长度、table 原子、句界切分、重叠公式与截短场景。
</verification>

<success_criteria>
- CHUNK-02 的规则全部有自动化断言；
- D-20~D-23 全量落地，无“简化版”或未来占位；
- chunker 核心可由 CLI 与后续阶段直接调用。
</success_criteria>
