---
phase: 02-ir
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/eval_corpus/cli.py
  - tests/test_chunker_cli.py
  - tests/fixtures/chunking/manual_blocks.json
autonomous: true
requirements:
  - CHUNK-03
must_haves:
  truths:
    - "同一 chunk 子命令可对统一 ParsedBlock[] 输入执行并产出 Chunk[] JSON。"
    - "CLI 参数支持 min/max/overlap 配置并执行范围校验，默认 300/1000/0.15。"
    - "元数据语义符合 D-24~D-26，且测试基于手工夹具不依赖 OCR。"
  artifacts:
    - path: "src/eval_corpus/cli.py"
      provides: "corpus-eval chunk 子命令入口与参数绑定"
    - path: "tests/test_chunker_cli.py"
      provides: "命令级 roundtrip 与配置校验测试"
    - path: "tests/fixtures/chunking/manual_blocks.json"
      provides: "手工 ParsedBlock[] 夹具样本"
  key_links:
    - from: "src/eval_corpus/cli.py"
      to: "src/eval_corpus/chunk_io.py"
      via: "--blocks-in 读取和模型校验"
      pattern: "blocks-in|TypeAdapter|model_validate"
    - from: "src/eval_corpus/cli.py"
      to: "src/eval_corpus/chunker.py"
      via: "chunk API 调用"
      pattern: "min-chars|max-chars|overlap-ratio"
---

<objective>
把统一分块核心接入现有 `corpus-eval` CLI，并通过手工夹具测试验证 CHUNK-03 的公平对比入口。

Purpose: 固化“统一输入 ParsedBlock[] -> 统一 chunk 输出”的执行路径，确保后续三工具适配仅负责产出 ParsedBlock 而不改分块逻辑。
Output: `chunk` 子命令、CLI 自动化测试、手工夹具文件。
</objective>

<context>
@.planning/phases/02-ir/02-CONTEXT.md
@.planning/phases/02-ir/02-RESEARCH.md
@.planning/phases/02-ir/02-02-PLAN.md
@src/eval_corpus/cli.py
@tests/
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 接入 chunk 子命令与配置校验</name>
  <read_first>src/eval_corpus/cli.py, src/eval_corpus/chunk_io.py, src/eval_corpus/chunker.py, .planning/phases/02-ir/02-CONTEXT.md</read_first>
  <files>src/eval_corpus/cli.py</files>
  <action>在现有 Typer CLI 中新增 `chunk` 子命令（D-28）：实现 --blocks-in/--chunks-out 参数，读取 ParsedBlock[] JSON，调用统一 chunk API 输出 Chunk[] JSON。新增 --min-chars/--max-chars/--overlap-ratio（默认 300/1000/0.15）并做范围校验（ratio 必须在 [0.10,0.20]），接口语义与 D-27 对齐。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_cli.py::test_chunk_command_roundtrip -x</automated>
  </verify>
  <acceptance_criteria>命令执行后生成 chunk JSON 文件；默认参数与显式参数均可生效；非法 overlap-ratio 返回明确错误信息。</acceptance_criteria>
  <done>cli.py 的 `chunk` 子命令可执行端到端分块，参数默认值与范围校验经自动化测试确认。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用手工 ParsedBlock 夹具覆盖元数据与公平性路径</name>
  <read_first>.planning/phases/02-ir/02-CONTEXT.md, tests/</read_first>
  <files>tests/fixtures/chunking/manual_blocks.json, tests/test_chunker_cli.py</files>
  <action>创建手工构造的 ParsedBlock[] 夹具并编写 CLI 测试（D-29）：覆盖 title/paragraph/table 混合、page 为 null/跨页、heading_path 数组、parser_tool 默认 unknown 等场景，验证输出 chunk 元数据保持 D-24~D-26 语义，同时确认该流程不依赖任意 OCR 工具私有输出（CHUNK-03 公平性）。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_cli.py -x</automated>
  </verify>
  <acceptance_criteria>测试只依赖手工夹具即可通过；输出 chunk 中 page/heading_path/parser_tool 语义正确；统一分块器可被后续三工具共享调用。</acceptance_criteria>
  <done>手工夹具与 CLI 测试完成，CHUNK-03 公平性路径固定为统一 ParsedBlock[] 输入与统一 chunk 输出。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 参数 -> 文件系统 | 用户输入路径与参数进入读写流程，可能触发错误覆盖或非法配置 |
| 统一 CLI 入口 -> 后续多工具对比 | 若入口逻辑偏向某工具格式会破坏公平性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `cli.py` 参数 | mitigate | 对 overlap/min/max 做边界校验并拒绝非法配置 |
| T-02-08 | Spoofing | parser_tool 元数据 | accept | Phase 2 固定 unknown（D-19），真实工具标识在 Phase 3 写入 |
| T-02-09 | Repudiation | CLI 输出一致性 | mitigate | 通过手工夹具回归测试固定输入输出行为，保证可复现 |
</threat_model>

<verification>
执行 CLI 级测试验证 CHUNK-03：统一输入、统一分块逻辑、统一产物语义。
</verification>

<success_criteria>
- CHUNK-03 自动化验证通过；
- D-27~D-29 全量落实，且与 D-16~D-26 语义兼容；
- Phase 3 仅需提供 ParsedBlock[] 即可复用本阶段分块器。
</success_criteria>
