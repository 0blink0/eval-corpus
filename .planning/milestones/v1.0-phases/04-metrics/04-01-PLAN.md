---
phase: 04-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eval_corpus/metrics/models.py
  - src/eval_corpus/metrics/calculators.py
  - tests/test_metrics_core.py
autonomous: true
requirements:
  - METR-01
  - METR-02
  - METR-03
  - METR-04
  - METR-05
  - METR-06
must_haves:
  truths:
    - "同一批 chunk 输入可稳定计算 METR-01~06，且每项都有分子/分母/排除条件（D-44）"
    - "不可判定样本不会被记为 0 分，而是进入 not_applicable 并记录 applicable_count/total_count（D-45, D-52, D-53）"
    - "每项指标均输出 raw_value + threshold + level(pass/warn/fail)（D-48, D-49）"
  artifacts:
    - path: src/eval_corpus/metrics/models.py
      provides: 指标结果与阈值契约（含 applicability 字段）
    - path: src/eval_corpus/metrics/calculators.py
      provides: METR-01~06 原子计算函数
    - path: tests/test_metrics_core.py
      provides: METR-01~06 口径回归测试
  key_links:
    - from: src/eval_corpus/metrics/calculators.py
      to: src/eval_corpus/metrics/models.py
      via: MetricResult/Threshold 模型返回
      pattern: "from eval_corpus\\.metrics\\.models import"
    - from: tests/test_metrics_core.py
      to: src/eval_corpus/metrics/calculators.py
      via: pytest 单测直接调用指标函数
      pattern: "from eval_corpus\\.metrics\\.calculators import"
---

<objective>
建立 Phase 4 指标引擎的核心口径层：先落地 METR-01~06 的统一模型与计算函数，确保严格可审计。

Purpose: 把“分块质量”从口头定义转为可复现、可回归、可解释的数据契约。  
Output: 指标模型文件、六项指标计算实现、对应核心测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics/04-CONTEXT.md
@.planning/phases/04-metrics/04-RESEARCH.md
@src/eval_corpus/ir_models.py
@src/eval_corpus/chunker.py
@src/eval_corpus/adapter_runner.py

<interfaces>
From src/eval_corpus/ir_models.py:
- ParsedBlock(type,text,page,heading_path,parser_tool,source_file,cells)
- Chunk(chunk_id,text,source_file,page,heading_path,parser_tool,block_types,page_span,overlap_truncated)
- ChunkConfig(min_chars,max_chars,overlap_ratio in [0.10,0.20])

From src/eval_corpus/adapter_runner.py:
- run_adapter_on_files(tool, files, fail_fast=False, debug=False) -> {tool, runtime_metadata, results, errors}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 METR-01~06 的测试基线（先红后绿）</name>
  <read_first>tests, src/eval_corpus/ir_models.py, .planning/phases/04-metrics/04-CONTEXT.md</read_first>
  <files>tests/test_metrics_core.py</files>
  <acceptance_criteria>测试文件包含 test_coverage_completeness/test_length_compliance/test_boundary_accuracy_and_hardcut/test_table_retention/test_overlap_compliance/test_metadata_completeness 六个用例名，可被 pytest -k metrics_core 发现。</acceptance_criteria>
  <action>新增 METR-01~06 对应测试；每个测试显式断言 numerator/denominator/excluded/applicable_count/total_count，按 D-44 与 D-45 固定口径编写，先允许失败以锁定行为。</action>
  <verify>
    <automated>pytest tests/test_metrics_core.py -q -x</automated>
  </verify>
  <done>六项指标行为被测试精确定义，执行结果能区分口径错误与实现错误。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现指标结果模型与阈值判级契约</name>
  <read_first>src/eval_corpus/ir_models.py, .planning/phases/04-metrics/04-CONTEXT.md, .planning/phases/04-metrics/04-RESEARCH.md</read_first>
  <files>src/eval_corpus/metrics/models.py</files>
  <acceptance_criteria>模型中存在可 grep 到的字段 level/raw_value/threshold/numerator/denominator/excluded_count/applicable_count/total_count/not_applicable_reasons，且 level 仅允许 pass|warn|fail。</acceptance_criteria>
  <action>创建 Pydantic v2 模型，统一表示指标值、阈值、等级与适用性；实现阈值比较函数输出 pass/warn/fail（D-48），并保留 raw 与阈值原值（D-49）；禁止将 not_applicable 合并为 0 分（D-45, D-52, D-53）。</action>
  <verify>
    <automated>python -c "from eval_corpus.metrics.models import MetricResult, MetricThreshold; print('ok')"</automated>
  </verify>
  <done>后续任何指标函数都可返回统一可审计结构，且等级逻辑集中维护。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 METR-01~06 原子计算函数并跑通单测</name>
  <read_first>src/eval_corpus/chunker.py, src/eval_corpus/ir_models.py, src/eval_corpus/metrics/models.py, tests/test_metrics_core.py</read_first>
  <files>src/eval_corpus/metrics/calculators.py, tests/test_metrics_core.py</files>
  <acceptance_criteria>calculators.py 中存在 compute_metric_01 至 compute_metric_06 六个导出函数；每个函数返回 MetricResult 并处理 not_applicable；pytest tests/test_metrics_core.py 全绿。</acceptance_criteria>
  <action>按 METR-01~06 分别实现：覆盖完整率、长度达标率、边界准确率/硬切比、表格保持率、重叠合理率、元数据完整率；每项显式定义分子/分母/排除逻辑（D-44），异常与缺失样本进入 not_applicable/errors 统计（D-52, D-53）。</action>
  <verify>
    <automated>pytest tests/test_metrics_core.py -q -x</automated>
  </verify>
  <done>METR-01~06 具备稳定计算能力，且对不可判定样本可解释。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk/adapter JSON -> metrics calculators | 非可信输入进入指标计算层 |
| metrics core -> test assertions | 口径变更可能导致“看似通过、实则偏移” |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | src/eval_corpus/metrics/calculators.py | mitigate | 用 Pydantic 模型限制输入结构并在函数入口校验必需字段 |
| T-04-02 | D | src/eval_corpus/metrics/calculators.py | mitigate | 对超长文本采用分块统计并避免全量拼接造成内存峰值 |
| T-04-03 | R | tests/test_metrics_core.py | mitigate | 每项指标固定断言分子分母与排除条件，防止口径漂移不可追溯 |
</threat_model>

<verification>
执行 `pytest tests/test_metrics_core.py -q -x`。  
若失败，优先检查 D-44/D-45 相关口径字段是否缺失或命名漂移。
</verification>

<success_criteria>
METR-01~06 的代码实现与测试口径一致；每项指标可输出 raw+threshold+level 与 applicability 统计。
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics/04-01-SUMMARY.md`
</output>
