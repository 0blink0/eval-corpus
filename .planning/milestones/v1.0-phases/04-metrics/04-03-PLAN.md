---
phase: 04-metrics
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/eval_corpus/metrics/aggregate.py
  - src/eval_corpus/metrics_io.py
  - src/eval_corpus/cli.py
  - tests/test_metrics_aggregate.py
  - tests/test_metrics_cli.py
autonomous: true
requirements:
  - METR-01
  - METR-02
  - METR-03
  - METR-04
  - METR-05
  - METR-06
  - METR-07
must_haves:
  truths:
    - "一次运行输出单一 JSON 工件，且包含 per_file/per_tool/overall 三层聚合（D-50）"
    - "Phase 4 仅以 JSON 为标准接口，不输出 CSV/HTML/Markdown（D-51）"
    - "聚合结果保留 raw+threshold+level 和 applicable/not_applicable/errors 统计（D-49, D-53）"
  artifacts:
    - path: src/eval_corpus/metrics/aggregate.py
      provides: 三层聚合逻辑
    - path: src/eval_corpus/metrics_io.py
      provides: 单工件 JSON 序列化与写出
    - path: src/eval_corpus/cli.py
      provides: metrics 子命令入口
    - path: tests/test_metrics_aggregate.py
      provides: 聚合行为测试
    - path: tests/test_metrics_cli.py
      provides: CLI 输出与参数测试
  key_links:
    - from: src/eval_corpus/cli.py
      to: src/eval_corpus/metrics/aggregate.py
      via: metrics command orchestration
      pattern: "app\\.command\\(\"metrics\"\\)"
    - from: src/eval_corpus/metrics/aggregate.py
      to: src/eval_corpus/metrics_io.py
      via: aggregated payload persistence
      pattern: "per_file|per_tool|overall"
---

<objective>
完成指标引擎对外可执行接口：聚合、落盘、CLI 集成，产出 Phase 4 标准 JSON 工件。

Purpose: 将前两计划的指标计算能力组织为“可一键运行、可供 Phase 5 消费”的标准上游。  
Output: 聚合模块、metrics I/O、CLI 命令与端到端测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics/04-CONTEXT.md
@.planning/phases/04-metrics/04-RESEARCH.md
@src/eval_corpus/cli.py
@src/eval_corpus/adapter_runner.py
@src/eval_corpus/metrics/models.py
@src/eval_corpus/metrics/calculators.py
@src/eval_corpus/chunk_io.py

<interfaces>
Required payload shape per D-50:
- per_file: [{file, tool, metrics, errors, not_applicable}]
- per_tool: [{tool, metrics_summary, applicable_count, total_count}]
- overall: {metrics_summary, generated_at, runtime_metadata}

CLI pattern from src/eval_corpus/cli.py:
- @app.command("adapt")
- summary_out 写 JSON 并 stderr 打印摘要
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现三层聚合与聚合测试</name>
  <read_first>src/eval_corpus/metrics/models.py, src/eval_corpus/metrics/calculators.py, .planning/phases/04-metrics/04-CONTEXT.md</read_first>
  <files>src/eval_corpus/metrics/aggregate.py, tests/test_metrics_aggregate.py</files>
  <acceptance_criteria>aggregate.py 中存在 build_per_file_metrics/build_per_tool_metrics/build_overall_metrics 或等效函数；测试断言输出包含 per_file/per_tool/overall 三个顶层键并校验 level/raw/threshold 字段存在。</acceptance_criteria>
  <action>实现聚合器：按文件汇总指标、按工具再聚合、生成 overall 汇总；所有层级保留 raw_value、threshold、level 与 applicable 统计；不得引入 CSV/HTML/Markdown 分支（D-51）。</action>
  <verify>
    <automated>pytest tests/test_metrics_aggregate.py -q -x</automated>
  </verify>
  <done>聚合层可稳定生成 D-50 约定的三层结构。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 metrics JSON I/O 与 CLI metrics 命令</name>
  <read_first>src/eval_corpus/cli.py, src/eval_corpus/adapter_runner.py, src/eval_corpus/chunk_io.py, src/eval_corpus/metrics/aggregate.py</read_first>
  <files>src/eval_corpus/metrics_io.py, src/eval_corpus/cli.py</files>
  <acceptance_criteria>cli.py 中可 grep 到 @app.command("metrics")；命令支持 --input / --out 参数；输出 JSON 顶层含 per_file/per_tool/overall；无 CSV/HTML/Markdown 输出分支。</acceptance_criteria>
  <action>新增 `metrics` 子命令：读取统一输入、调用指标计算与聚合、写单一 JSON 工件；stderr 打印摘要（tool/files/errors）。显式记录 generated_at/runtime_metadata 以支持审计追溯。</action>
  <verify>
    <automated>python -m eval_corpus.cli metrics --help</automated>
  </verify>
  <done>用户可通过单命令生成 Phase 4 标准 JSON 工件。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补齐 CLI 端到端测试并完成 Phase 4 验收</name>
  <read_first>tests/test_metrics_aggregate.py, src/eval_corpus/cli.py, src/eval_corpus/metrics_io.py</read_first>
  <files>tests/test_metrics_cli.py</files>
  <acceptance_criteria>test_metrics_cli.py 覆盖命令成功路径、参数错误路径、输出结构校验；`pytest tests/test_metrics_*.py -q` 全绿。</acceptance_criteria>
  <action>使用固定夹具构造最小输入，验证 metrics 命令可生成单 JSON；断言每项指标保留 raw+threshold+level，且包含 not_applicable/errors 统计，不输出延后特性。</action>
  <verify>
    <automated>pytest tests/test_metrics_*.py -q</automated>
  </verify>
  <done>Phase 4 从函数级到 CLI 级全部可回归，满足执行与交付条件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input path -> metrics pipeline | 用户输入路径属于非可信边界 |
| metrics payload -> phase5 consumer | 上游结构漂移会直接破坏下游报告生成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | I | src/eval_corpus/cli.py | mitigate | 仅接受显式输入输出路径并在写出前创建目标目录，避免意外覆盖 |
| T-04-08 | R | src/eval_corpus/metrics/aggregate.py | mitigate | 聚合层强制保留 raw+threshold+level 与 applicable 统计，防止不可追溯 |
| T-04-09 | D | src/eval_corpus/metrics_io.py | mitigate | 写出前校验数据结构并使用流式/分步构造，避免超大对象一次性序列化风险 |
</threat_model>

<verification>
执行 `pytest tests/test_metrics_*.py -q` 与 `pytest -q`。  
手工 spot-check：`python -m eval_corpus.cli metrics --help` 可见命令与参数说明。
</verification>

<success_criteria>
Phase 4 产出单一 JSON 标准接口，包含完整三层聚合与七项指标可追溯结果，可直接作为 Phase 5 输入。
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics/04-03-SUMMARY.md`
</output>
