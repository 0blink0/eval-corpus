---
phase: 05-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eval_corpus/reporting/models.py
  - src/eval_corpus/reporting/build.py
  - src/eval_corpus/reporting/exporters.py
  - tests/test_reporting_summary.py
  - tests/test_reporting_detail.py
  - tests/test_reporting_metadata.py
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
must_haves:
  truths:
    - "同一次运行可产出总对比表（三工具 x 七指标）并包含阈值列（D-54, D-56）"
    - "同一报告数据可导出 JSON/CSV/Markdown/HTML 且字段一致（D-55, D-57）"
    - "每工具明细含 per_file 指标与运行元信息附录，可追溯生成上下文（D-54, D-63）"
  artifacts:
    - path: "src/eval_corpus/reporting/models.py"
      provides: "统一 ReportPayload 与导出行模型"
    - path: "src/eval_corpus/reporting/exporters.py"
      provides: "JSON/CSV/Markdown/HTML 四格式导出器"
    - path: "tests/test_reporting_summary.py"
      provides: "总表与阈值列契约测试"
  key_links:
    - from: "src/eval_corpus/metrics_io.py"
      to: "src/eval_corpus/reporting/build.py"
      via: "per_file/per_tool/overall 到 ReportPayload 映射"
      pattern: "per_file|per_tool|overall"
    - from: "src/eval_corpus/reporting/build.py"
      to: "src/eval_corpus/reporting/exporters.py"
      via: "统一模型驱动四格式导出"
      pattern: "export_(json|csv|markdown|html)"
---

<objective>
交付报告层核心能力：将 Phase 4 指标工件映射为统一报告模型，并一次导出 JSON/CSV/Markdown/HTML。

Purpose: 满足 RPT-01/RPT-02/RPT-03，确保“机器可读稳定 + 人读视图一致”（D-55, D-57）。
Output: 报告模型、导出器、报告层测试基线。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reporting/05-CONTEXT.md
@.planning/phases/05-reporting/05-RESEARCH.md
@src/eval_corpus/metrics_io.py
@src/eval_corpus/metrics/aggregate.py

<interfaces>
From src/eval_corpus/metrics_io.py:
```python
def build_metrics_artifact(adapter_summary: dict[str, Any]) -> dict[str, Any]:
    payload = {
        "per_file": per_file,
        "per_tool": per_tool,
        "overall": overall,
    }
```

From src/eval_corpus/cli.py:
```python
@app.command("metrics")
def metrics(input_path: Path, out_path: Path) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立统一报告数据模型与构建器</name>
  <files>src/eval_corpus/reporting/models.py, src/eval_corpus/reporting/build.py, tests/test_reporting_summary.py</files>
  <behavior>
    - Test 1: 给定 Phase 4 payload，构建后总表固定包含 METR-01..METR-07 与 threshold 列（D-54）。
    - Test 2: 三工具行与 overall 汇总行字段集合一致，排序可复现（D-57）。
  </behavior>
  <action>新增 ReportPayload/ReportRow/RuntimeAppendix 模型，并实现从 per_file/per_tool/overall 到 canonical 模型映射；严格消费现有指标工件，不在报告层重算指标（per D-57）。字段缺失时抛出结构化错误，避免静默降级。</action>
  <verify>
    <automated>pytest tests/test_reporting_summary.py -q</automated>
  </verify>
  <done>报告构建器能稳定生成总对比表数据与附录元信息输入结构，测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现四格式导出器与每工具明细输出</name>
  <files>src/eval_corpus/reporting/exporters.py, tests/test_reporting_detail.py, tests/test_reporting_metadata.py</files>
  <behavior>
    - Test 1: 同一 ReportPayload 导出 JSON/CSV/Markdown/HTML 的指标值一致（D-56, D-57）。
    - Test 2: 每工具明细包含 per_file raw_value/threshold/level 与 errors/not_applicable 统计（D-54）。
    - Test 3: 运行元信息附录包含 generated_at/tool_versions/git_commit 字段（D-54, D-63）。
  </behavior>
  <action>实现 `export_json/export_csv/export_markdown/export_html` 四个导出函数；所有导出从同一 canonical 模型读取，禁止每格式重复拼接字段（per D-57）。HTML 输出对文本字段做 escape，避免注入风险。</action>
  <verify>
    <automated>pytest tests/test_reporting_detail.py tests/test_reporting_metadata.py -q</automated>
  </verify>
  <done>四格式导出可用且跨格式字段一致，每工具明细与运行附录完整。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics artifact -> reporting builder | 外部输入 JSON 进入报告模型，可能含畸形字段 |
| report payload -> html exporter | 未转义文本进入 HTML 视图 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `reporting/build.py` | mitigate | 用模型与必填键校验输入，仅接受 `per_file/per_tool/overall` |
| T-05-02 | I | `reporting/exporters.py` | mitigate | HTML 导出统一 escape 动态文本，避免注入 |
| T-05-03 | R | `reporting/exporters.py` | mitigate | 在附录落盘 `generated_at/tool_versions/git_commit/run_id` 便于审计 |
</threat_model>

<verification>
运行 `pytest tests/test_reporting_summary.py tests/test_reporting_detail.py tests/test_reporting_metadata.py -q`，并验证生成样例的四格式指标值一致。
</verification>

<success_criteria>
RPT-01/RPT-02/RPT-03 的报告层能力完成：总表、明细、运行元信息附录均可从单一模型导出到四种格式。
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting/05-01-SUMMARY.md`
</output>
