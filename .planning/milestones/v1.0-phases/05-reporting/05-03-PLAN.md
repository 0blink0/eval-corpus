---
phase: 05-reporting
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/eval_corpus/reporting/runtime.py
  - src/eval_corpus/reporting/layout.py
  - src/eval_corpus/cli.py
  - docs/phase5_batch_runbook.md
  - tests/test_reporting_cli.py
autonomous: true
requirements:
  - RPT-03
  - DATA-02
must_haves:
  truths:
    - "一次命令可在 run_id+时间戳目录下落盘报告、明细、日志和中间文件（D-62, D-63）"
    - "批跑入口暴露并行度、失败阈值、重试参数并支持目录递归（D-58, D-59, D-65）"
    - "运行手册覆盖单机执行与云端扩跑步骤，且不引入分布式队列（D-58）"
  artifacts:
    - path: "src/eval_corpus/cli.py"
      provides: "report/synthetic/batch 子命令入口"
    - path: "src/eval_corpus/reporting/layout.py"
      provides: "run 目录结构与命名规则实现"
    - path: "docs/phase5_batch_runbook.md"
      provides: "批跑与云端扩跑操作手册"
  key_links:
    - from: "src/eval_corpus/cli.py"
      to: "src/eval_corpus/reporting/exporters.py"
      via: "CLI report 命令触发四格式导出"
      pattern: "@app.command\\(\"report\"\\)"
    - from: "src/eval_corpus/cli.py"
      to: "src/eval_corpus/batch/runner.py"
      via: "CLI batch 命令触发目录批跑"
      pattern: "@app.command\\(\"batch\"\\)"
---

<objective>
完成 Phase 5 交付闭环：将报告/生成器/批跑能力接入 CLI，并固化 run 目录规范与操作手册。

Purpose: 把 05-01 与 05-02 的模块能力转化为“一次命令可执行”的最终交付（D-58, D-62）。
Output: CLI 子命令、run 目录布局工具、运行手册与 CLI 集成测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reporting/05-CONTEXT.md
@.planning/phases/05-reporting/05-01-PLAN.md
@.planning/phases/05-reporting/05-02-PLAN.md
@src/eval_corpus/cli.py

<interfaces>
From src/eval_corpus/cli.py:
```python
app = typer.Typer(help="Corpus evaluation CLI: manifest + golden stats for parsing experiments.")

@app.command("metrics")
def metrics(input_path: Path, out_path: Path) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 run 目录布局与运行元信息采集</name>
  <files>src/eval_corpus/reporting/runtime.py, src/eval_corpus/reporting/layout.py, tests/test_reporting_cli.py</files>
  <behavior>
    - Test 1: 每次执行生成 `run_id + timestamp` 根目录并含 tool/artifact 双维子目录（D-62, D-63）。
    - Test 2: runtime 元信息包含 generated_at/tool_versions/git_commit/git_status（D-54, D-62）。
  </behavior>
  <action>实现 run 目录工厂与元信息收集函数；目录结构固定化并可复用给 report/batch/synthetic 命令。若 git 信息不可用，按约定输出 `git_commit: null` 与 `git_status: unavailable`，禁止抛出未处理异常。</action>
  <verify>
    <automated>pytest tests/test_reporting_cli.py -q</automated>
  </verify>
  <done>run 目录与元信息契约完成并通过自动化验证。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 CLI 子命令并打通端到端落盘</name>
  <files>src/eval_corpus/cli.py, tests/test_reporting_cli.py</files>
  <behavior>
    - Test 1: `report` 命令消费 metrics artifact 并导出四格式与附录（D-56, D-57）。
    - Test 2: `batch` 命令支持目录递归与并行度/阈值/重试参数（D-58, D-65）。
    - Test 3: `synthetic-data` 命令可生成三类型样本并用于后续批跑（D-60, D-61）。
  </behavior>
  <action>在现有 Typer app 增加 `report`、`batch`、`synthetic-data` 子命令，串联 05-01/05-02 模块。保持单机执行模型，不新增消息队列、分布式执行后端或远端调度 SDK（per D-59）。命令退出码遵循现有 CLI 约定：参数/输入错误返回 2，运行失败返回 1。</action>
  <verify>
    <automated>pytest tests/test_reporting_cli.py tests/test_metrics_cli.py -q</automated>
  </verify>
  <done>CLI 新命令可执行并产出完整 run 工件，回归测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 编写批跑与云端扩跑运行手册</name>
  <files>docs/phase5_batch_runbook.md</files>
  <action>编写 runbook：依赖安装、report/synthetic-data/batch 命令示例、并行/阈值/重试参数说明、常见故障排查、云主机扩跑步骤。明确“本阶段不包含分布式队列，仅单机并发 + 目录切分”边界（per D-58, deferred ideas）。</action>
  <verify>
    <automated>python -m eval_corpus.cli --help</automated>
  </verify>
  <done>手册可支持新用户在本地或云主机复现实验批跑流程。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI input -> command handlers | 命令参数与路径来自不可信输入 |
| runtime metadata -> report artifacts | 元信息进入导出文件，影响审计与可追溯性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | S | `cli.py` | mitigate | 命令参数显式校验与 exit code 约定，拒绝非法输入 |
| T-05-08 | R | `reporting/runtime.py` | mitigate | 强制写入 run_id/时间戳/tool_versions/git 状态，保证审计链 |
| T-05-09 | D | `cli.py` + `batch/runner.py` | mitigate | 暴露并限制并行度与失败阈值，避免无限重试造成阻塞 |
</threat_model>

<verification>
执行 `pytest tests/test_reporting_cli.py tests/test_metrics_cli.py -q`，并手工检查一次 `report + synthetic-data + batch` 命令链可在 run 目录完整落盘。
</verification>

<success_criteria>
Phase 5 最终交付可由单次 CLI 流程触发，且工件目录、元信息、批跑手册均满足可复现与可追溯要求。
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting/05-03-SUMMARY.md`
</output>
