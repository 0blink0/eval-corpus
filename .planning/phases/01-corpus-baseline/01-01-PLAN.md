---
phase: 01-corpus-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - src/eval_corpus/__init__.py
  - src/eval_corpus/cli.py
  - src/eval_corpus/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - CORP-01
user_setup: []
must_haves:
  truths:
    - 未设置根路径时进程以非零码退出且 stderr 含 EVAL_CORPUS_ROOT 与 --root 提示
    - 设置有效目录时 `resolve_corpus_root` 返回绝对 pathlib.Path
  artifacts:
    - pyproject.toml 存在且定义 console_script `corpus-eval`
    - tests/test_config.py 存在且 pytest 可通过
  key_links:
    - CLI 参数优先于环境变量 EVAL_CORPUS_ROOT（与 D-02 一致）
---

<objective>
初始化 Python 包、依赖与 CLI 骨架；实现语料根路径解析（CLI `--root` 与环境变量 `EVAL_CORPUS_ROOT`）及缺失/非法路径时的明确错误（stderr + 非零退出），满足 CORP-01 与 D-01～D-04。
</objective>

<execution_context>
@C:\Users\一枚blink\.cursor\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/01-corpus-baseline/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 项目骨架与依赖</name>
  <read_first>
    - .planning/phases/01-corpus-baseline/01-CONTEXT.md（D-01～D-04）
    - .planning/phases/01-corpus-baseline/01-RESEARCH.md（Standard Stack）
  </read_first>
  <files>pyproject.toml, README.md, src/eval_corpus/__init__.py, src/eval_corpus/cli.py, tests/__init__.py, tests/conftest.py</files>
  <behavior>
    - `pyproject.toml` 含 `project.name`、`requires-python >= 3.11`、依赖 `typer[all]`、`pypdf`、`pytest`（dev）
    - `[project.scripts]` 映射 `corpus-eval = "eval_corpus.cli:app"`（若用 Typer 的 `typer.run` 则调整为可调用对象）
    - `README.md` 含「安装」「EVAL_CORPUS_ROOT」「`corpus-eval --help`」三节，并**显式**写明示例语料名为 **北燃热力宣传品采购归档资料**、实际路径由用户通过 `--root` / `EVAL_CORPUS_ROOT` 配置（与 CORP-01、PROJECT.md 一致）
  </behavior>
  <action>
    创建 `src/eval_corpus/cli.py`：`typer.Typer()` 应用名为根命令，暂挂空子命令或 `version`；确保 `python -m pytest` 能发现 `tests/`。
    包名目录使用 `eval_corpus`（与 import 一致）。
  </action>
  <verify>
    <automated>python -m pip install -e ".[dev]" && python -c "import eval_corpus.cli" && corpus-eval --help</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` 包含字符串 `[project.scripts]` 与 `corpus-eval`
    - `README.md` 包含字符串 `EVAL_CORPUS_ROOT` 与 `北燃热力宣传品采购归档资料`
    - `<automated>` 已成功执行（覆盖 `corpus-eval --help` 退出码）
  </acceptance_criteria>
  <done>可编辑安装成功，`corpus-eval --help` 可用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 根路径解析与校验</name>
  <read_first>
    - src/eval_corpus/cli.py
    - .planning/phases/01-corpus-baseline/01-CONTEXT.md
  </read_first>
  <files>src/eval_corpus/config.py, src/eval_corpus/cli.py, tests/test_config.py</files>
  <behavior>
    - `resolve_corpus_root(cli_root: str | None) -> Path`：若 `cli_root` 非空用它，否则读 `os.environ["EVAL_CORPUS_ROOT"]`；都空则 `raise ConfigError`（或返回 None 由 CLI 处理）
    - 路径必须存在且为目录；否则清晰错误信息
    - CLI 选项：`--root TEXT` 可选；根命令或 `manifest` 前共享该选项（若尚未有子命令，则在根 Typer 上加 `callback` 或全局 Option）
  </behavior>
  <action>
    实现 `config.py`：`resolve_corpus_root(cli_root: str | None) -> Path`，展开为绝对路径。无根路径时在 CLI 层 `typer.secho(..., err=True)` 输出中英文说明「设置 --root 或环境变量 EVAL_CORPUS_ROOT」，`raise typer.Exit(2)`。路径不存在或非目录：`Exit(2)` 与明确 stderr。
    `tests/test_config.py`：monkeypatch 环境变量与 `tmp_path`，覆盖优先顺序 CLI > env、缺失、不存在路径。
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_config.py` 中至少 3 个用例：仅 env 成功、CLI 覆盖 env、双缺失退出/异常
    - `src/eval_corpus/config.py` 含函数名 `resolve_corpus_root`
  </acceptance_criteria>
  <done>CORP-01 路径配置与失败路径有自动化测试覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户主机 → CLI | 用户主动传入本地/挂载路径；工具只读元数据与后续阶段内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | 解析路径 | accept | 用户负责语料完整性；工具不写入语料目录 |
| T-01-02 | Information disclosure | stderr 错误信息 | mitigate | 仅打印用户已提供的路径字符串；文档提醒勿在共享屏幕暴露敏感路径 |
| T-01-03 | Denial of service | 超大目录 | mitigate | 后续 scan 可记录上限；本计划仅校验根存在性 |

</threat_model>

<verification>
`pytest -q tests/test_config.py` 全绿；`corpus-eval --help` 可运行。
</verification>

<success_criteria>
- CORP-01 的配置入口与错误行为与 CONTEXT D-01～D-04 一致
- 自动化测试覆盖优先顺序与失败路径
</success_criteria>

<output>
完成后创建 `.planning/phases/01-corpus-baseline/01-01-SUMMARY.md`
</output>
