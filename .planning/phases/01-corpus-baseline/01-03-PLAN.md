---
phase: 01-corpus-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/eval_corpus/stats.py
  - src/eval_corpus/cli.py
  - tests/test_stats.py
  - README.md
autonomous: true
requirements:
  - CORP-03
user_setup: []
must_haves:
  truths:
    - 黄金统计 JSON 含至少：`total_files`, `total_unicode_chars`；PDF 含 `page_count` 或 `page_count_reason`
    - 扫描件/空文本层 PDF：`needs_ocr: true` 且不伪造正文长度（D-15）
    - 表格计数字段带 `heuristic: true`（D-13）
  artifacts:
    - `tests/test_stats.py` 使用 pypdf 或最小 PDF 字节夹具验证页数与 needs_ocr
  key_links:
    - 字符计数为 Unicode 字符（D-14）
---

<objective>
在清单扫描能力之上，计算黄金统计：文件数、Unicode 总字符、按文件的页数/原因、启发式表格数、文本层基准与 `needs_ocr`；输出 JSON 工件并可由 CLI `stats` 调用，满足 CORP-03 与 D-12～D-15。
</objective>

<execution_context>
@C:\Users\一枚blink\.cursor\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/phases/01-corpus-baseline/01-CONTEXT.md
@src/eval_corpus/scan.py
@src/eval_corpus/manifest.py
@src/eval_corpus/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统计核心逻辑</name>
  <read_first>
    - .planning/phases/01-corpus-baseline/01-CONTEXT.md（D-12～D-15）
    - src/eval_corpus/scan.py
  </read_first>
  <files>src/eval_corpus/stats.py, tests/test_stats.py</files>
  <behavior>
    - `.txt`/`.md`：`read_text(errors="replace")`，`len(text)` 为字符数
    - `.pdf`：`pypdf.PdfReader` → `len(pages)`；拼接 `extract_text()`；若 `pages>0` 且拼接文本去空白后为空 → `needs_ocr: true`
    - `.xlsx`/`.xls`：表格启发式：`table_count_heuristic=1`（若仅扩展名匹配）、`heuristic: true`；无法读时 `table_count: null`, `reason`
    - 非文本二进制：字符数 0，可选 `skipped: true`
  </behavior>
  <action>
    实现 `compute_file_stats(path: Path) -> dict` 与 `aggregate_golden_stats(root, files) -> dict`，顶层 `schema_version: "1.0"`，`totals` 与 `files` 列表。
    字段命名 snake_case，与后续 Phase 消费方在 README 中列表说明。
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_stats.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_stats.py` 覆盖：纯文本字符数、最小 PDF（可用 pypdf 空白页创建）页数与 `needs_ocr`
    - `src/eval_corpus/stats.py` 含 `needs_ocr` 字符串（字段名）
  </acceptance_criteria>
  <done>黄金统计核心与单元测试就绪。</done>
</task>

<task type="auto">
  <name>Task 2: stats CLI 与文档</name>
  <read_first>
    - src/eval_corpus/stats.py
    - src/eval_corpus/cli.py
  </read_first>
  <files>src/eval_corpus/cli.py, README.md</files>
  <action>
    添加 Typer 子命令 `stats`：`--root`、`--stats-out` 默认 `golden_stats.json`（与清单默认模式对齐，文档说明）；流程：解析根 → 扫描 → 聚合 → 写 JSON → stderr 人类摘要（总文件数、总字符、需 OCR 文件数）。
    更新 `README.md`：`corpus-eval stats` 示例与输出字段表。
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_stats.py && corpus-eval stats --help</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` 含 `golden_stats` 或 `golden_stats.json` 字样
    - `src/eval_corpus/cli.py` 包含字符串 `stats`（子命令注册）
    - `<automated>` 已通过（覆盖 `corpus-eval stats --help`）
  </acceptance_criteria>
  <done>CORP-03 可通过单命令产出 JSON + stderr 摘要。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户语料 → 解析 | 仅统计与轻量解析；不对外传输 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of service | 超大 PDF | mitigate | 单文件可设页数/读取超时或文档说明风险；首版可记录 warning |
| T-01-07 | Information disclosure | stats JSON | accept | 仅本地写文件；提醒勿提交敏感统计路径 |

</threat_model>

<verification>
`pytest -q tests/`；`corpus-eval stats --help` 成功。
</verification>

<success_criteria>
- CORP-03 字段与 D-12～D-15 行为在测试与 README 可对齐
- 与 Phase 2+ 的 schema 版本字段一致策略已建立

</success_criteria>

<output>
完成后创建 `.planning/phases/01-corpus-baseline/01-03-SUMMARY.md`
</output>
