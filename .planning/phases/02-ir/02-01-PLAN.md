---
phase: 02-ir
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/eval_corpus/ir_models.py
  - src/eval_corpus/chunk_io.py
autonomous: true
requirements:
  - CHUNK-01
must_haves:
  truths:
    - "ParsedBlock 和 Chunk 使用 Pydantic v2 定义并可做 JSON 往返校验。"
    - "IR 字段语义符合 D-16~D-19 与 D-24~D-26，未知页码显式为 null。"
    - "统一输入边界仅接受 list[ParsedBlock]，为后续统一分块器提供稳定契约。"
  artifacts:
    - path: "src/eval_corpus/ir_models.py"
      provides: "ParsedBlock、Chunk、ChunkConfig 等 Pydantic v2 模型与字段约束"
    - path: "src/eval_corpus/chunk_io.py"
      provides: "TypeAdapter(list[ParsedBlock]) 的 JSON 读写与导出封装"
    - path: "pyproject.toml"
      provides: "pydantic>=2 运行时依赖声明"
  key_links:
    - from: "src/eval_corpus/chunk_io.py"
      to: "src/eval_corpus/ir_models.py"
      via: "TypeAdapter(list[ParsedBlock])"
      pattern: "TypeAdapter\\(list\\[ParsedBlock\\]\\)"
    - from: "pyproject.toml"
      to: "src/eval_corpus/ir_models.py"
      via: "runtime dependency"
      pattern: "pydantic"
---

<objective>
落地 Phase 2 的统一 IR 契约，确保 ParsedBlock/Chunk 的字段、序列化和校验规则完全对齐 D-16~D-19、D-24~D-26。

Purpose: 为 CHUNK-01 提供可复用、可验证、可序列化的单一事实来源，避免 Phase 3/4 出现模型漂移。
Output: Pydantic v2 IR 模型、JSON I/O 封装、依赖更新。
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-ir/02-CONTEXT.md
@.planning/phases/02-ir/02-RESEARCH.md
@src/eval_corpus/cli.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立 Pydantic v2 IR 模型与字段约束</name>
  <read_first>.planning/phases/02-ir/02-CONTEXT.md, .planning/REQUIREMENTS.md, src/eval_corpus/</read_first>
  <files>pyproject.toml, src/eval_corpus/ir_models.py</files>
  <action>在 pyproject.toml 增加运行时依赖 pydantic>=2（落实 D-16）；在 ir_models.py 定义 ParsedBlock/Chunk/ChunkConfig（BaseModel），并明确：type 枚举含 title/paragraph/table/other（D-18）、parser_tool 默认 "unknown"（D-19）、heading_path 为 list[str]（D-24）、page 可空且不使用 0 冒充未知（D-25）、chunk page 跨页取最小页并可预留 page_span 可选字段（D-26）。字段命名统一 snake_case（D-17）。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_core.py::test_parsed_block_schema -x</automated>
  </verify>
  <acceptance_criteria>模型可直接 validate_python / model_validate_json；table 类型强制存在 text；未知页码输出为 null；依赖安装后 import pydantic 成功。</acceptance_criteria>
  <done>ir_models.py 中 ParsedBlock/Chunk/ChunkConfig 可被导入并通过测试断言，D-16~D-19 与 D-24~D-26 语义均有可执行校验。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 ParsedBlock 列表 JSON 边界与导出</name>
  <read_first>src/eval_corpus/ir_models.py, src/eval_corpus/stats.py, .planning/phases/02-ir/02-RESEARCH.md</read_first>
  <files>src/eval_corpus/chunk_io.py</files>
  <action>新增 TypeAdapter(list[ParsedBlock]) 的读取与导出函数，统一使用 model_validate_json / dump_json（或等价模型导出）并保持 UTF-8 语义（ensure_ascii=False 风格）；明确交换格式为 JSON、snake_case（D-16/D-17），确保后续分块器入口只接收 ParsedBlock[]（为 D-27 做接口前置）。</action>
  <verify>
    <automated>pytest -q tests/test_chunker_core.py::test_parsed_block_json_roundtrip -x</automated>
  </verify>
  <acceptance_criteria>给定手工 ParsedBlock[] JSON 能稳定读入并导出；非法字段返回可诊断错误；输出 JSON 可被后续 chunk API 直接消费。</acceptance_criteria>
  <done>chunk_io.py 提供稳定的 ParsedBlock[] 入站/出站边界，非法 JSON 在测试中触发可定位错误，合法样本可 roundtrip。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON 输入 -> Pydantic 模型 | 非可信 blocks JSON 进入类型系统，可能携带畸形字段或错误类型 |
| 本地文件路径 -> I/O | 输入输出路径可能被误配导致读写失败或覆盖风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `chunk_io.py` JSON 入站 | mitigate | 使用 `TypeAdapter(list[ParsedBlock])` 严格校验并在错误时拒绝继续处理 |
| T-02-02 | DoS | 超大/畸形 JSON | mitigate | 在读取流程增加显式异常处理与错误提示，避免静默失败和无限重试 |
| T-02-03 | Repudiation | 产物可追溯性 | accept | Phase 2 先确保 schema_version 与字段稳定，运行级追溯在后续报告阶段补齐 |
</threat_model>

<verification>
执行 CHUNK-01 相关核心测试并保证失败信息可定位到字段级别。
</verification>

<success_criteria>
- CHUNK-01 可由自动化测试验证通过；
- D-16~D-19、D-24~D-26 在模型层均有明确实现；
- 后续计划可直接复用 ParsedBlock[] 边界，无需额外转换。
</success_criteria>
