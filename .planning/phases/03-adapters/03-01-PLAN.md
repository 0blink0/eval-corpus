---
phase: 03-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eval_corpus/adapters/base.py
  - src/eval_corpus/adapters/paddle.py
  - src/eval_corpus/adapters/registry.py
  - src/eval_corpus/adapter_runner.py
  - src/eval_corpus/cli.py
  - tests/fixtures/adapters/README.md
  - tests/test_adapters_contract.py
  - tests/test_adapters_paddle.py
autonomous: true
requirements:
  - ADPT-01
must_haves:
  truths:
    - "PaddleOCR 链路可对同一夹具输出合法 ParsedBlock[] 或标准化可定位错误。"
    - "统一接口和统一错误模型对后续两工具保持可复用。"
    - "CLI 在默认 continue-on-error 下可汇总失败文件，不中断整批。"
  artifacts:
    - path: "src/eval_corpus/adapters/base.py"
      provides: "D-30/D-31 统一适配器协议、AdapterConfig、AdapterError"
    - path: "src/eval_corpus/adapters/paddle.py"
      provides: "PaddleOCR 输出到 ParsedBlock[] 的归一化实现"
    - path: "tests/test_adapters_contract.py"
      provides: "接口契约、错误模型、continue-on-error/fail-fast 行为测试"
  key_links:
    - from: "src/eval_corpus/adapters/paddle.py"
      to: "src/eval_corpus/ir_models.py"
      via: "ParsedBlock 构造和字段校验"
      pattern: "ParsedBlock\\("
    - from: "src/eval_corpus/cli.py"
      to: "src/eval_corpus/adapter_runner.py"
      via: "parse/adapt 子命令编排"
      pattern: "fail-fast|continue"
---

<objective>
打通 Phase 3 的第一条真实链路（PaddleOCR），并先落地三工具共用的接口、错误模型、编排策略与最小夹具契约。

Purpose: 先把 D-30~D-35、D-38~D-42 的基础规则固定下来，确保后续 GLM/MinerU 仅新增适配器实现而不重改架构。  
Output: 可复用 adapter contract + Paddle adapter + 统一 runner/CLI 行为 + 对应测试与夹具约束。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapters/03-CONTEXT.md
@.planning/phases/03-adapters/03-RESEARCH.md
@src/eval_corpus/ir_models.py
@src/eval_corpus/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化统一适配器接口与错误模型（D-30, D-31, D-33）</name>
  <read_first>
    src/eval_corpus/ir_models.py
    .planning/phases/03-adapters/03-CONTEXT.md
  </read_first>
  <files>
    src/eval_corpus/adapters/base.py
    src/eval_corpus/adapters/registry.py
    tests/test_adapters_contract.py
  </files>
  <action>
    定义 `parse_to_blocks(file_path: Path, config: AdapterConfig) -&gt; list[ParsedBlock]` 统一协议（per D-30）；定义可序列化 `AdapterError`，字段固定为 `stage,file,tool,message,raw_error` 且 `stage` 枚举仅允许 `load|parse|normalize|validate`（per D-31）。在 contract 测试中用具体样例断言最低一致语义：`type` 仅允许 `title|paragraph|table|other`、`text` 必填、`page` 可为 null、`heading_path` 缺失回退 `[]`（per D-33）。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_contract.py::test_adapter_contract_and_error_schema -x</automated>
  </verify>
  <acceptance_criteria>
    统一接口和错误模型可被测试稳定加载；非法 stage/type 会被拒绝；最低一致语义在测试中有显式断言。
  </acceptance_criteria>
  <done>`base.py` 和 `registry.py` 存在并可导入；契约测试通过且失败信息可定位字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Paddle 适配器归一化出口（D-34, D-35, D-40）</name>
  <read_first>
    src/eval_corpus/adapters/base.py
    src/eval_corpus/ir_models.py
    .planning/phases/03-adapters/03-RESEARCH.md
  </read_first>
  <files>
    src/eval_corpus/adapters/paddle.py
    tests/test_adapters_paddle.py
    tests/fixtures/adapters/README.md
  </files>
  <action>
    在 adapter 内部消化 Paddle 原始结构并仅输出统一 `ParsedBlock[]`（per D-34）；写入真实 `parser_tool="paddleocr"` 替代 unknown（per D-35）。当依赖缺失、解析失败、归一化失败或校验失败时，分别映射到 `stage=load|parse|normalize|validate`，错误输出至少含文件路径、工具名、阶段、简明消息，debug 才附 `raw_error`（per D-40）。夹具 README 明确最小三件套规范（文本 PDF/扫描 PDF/表格样例，per D-41/D-42）。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_paddle.py -x</automated>
  </verify>
  <acceptance_criteria>
    Paddle 成功路径可返回合法 ParsedBlock 列表；失败路径均返回标准化错误且 stage 正确；最小三件套夹具规范被文档化并可供后续工具复用。
  </acceptance_criteria>
  <done>Paddle 测试通过；`parser_tool` 为真实工具名；无工具私有结构泄露到适配器出口。</done>
</task>

<task type="auto">
  <name>Task 3: 接入统一 runner 与 CLI 编排策略（D-32, D-38, D-39）</name>
  <read_first>
    src/eval_corpus/cli.py
    src/eval_corpus/adapters/registry.py
    tests/test_adapters_contract.py
  </read_first>
  <files>
    src/eval_corpus/adapter_runner.py
    src/eval_corpus/cli.py
    tests/test_adapters_contract.py
  </files>
  <action>
    在 runner/CLI 层只做编排、汇总和退出码管理，不处理任何工具私有结构（per D-32）。默认启用 continue-on-error（per D-38），并提供 `--fail-fast` 开关使首个失败立即终止（per D-39）。为两种策略补充自动化测试：批量 3 文件输入下验证 continue 模式输出完整失败清单、fail-fast 模式在首错停止。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_contract.py::test_runner_continue_and_fail_fast -x</automated>
  </verify>
  <acceptance_criteria>
    CLI 参数行为与 D-38/D-39 一致；continue 模式可输出失败清单；fail-fast 模式有可预测退出行为；CLI 不包含工具私有映射分支。
  </acceptance_criteria>
  <done>runner 与 CLI 的策略可被自动化测试证明，且后续工具可直接复用该编排能力。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file -> adapter | 非受信文档输入进入解析流程 |
| adapter -> CLI summary | 异常与结果跨模块汇总，需防止信息丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | D | `adapters/paddle.py` | mitigate | 对 load/parse 设置超时与异常封装为 `AdapterError(stage=...)`，保证可中断和可定位 |
| T-03-02 | I | `adapter_runner.py` | mitigate | 错误日志默认不输出密钥等敏感信息，debug 模式对 raw_error 做脱敏 |
| T-03-03 | T | `adapters/base.py` | mitigate | 用枚举+Pydantic 约束 stage/type，拒绝非法字段漂移 |
</threat_model>

<verification>
运行：
- `pytest -q tests/test_adapters_contract.py -x`
- `pytest -q tests/test_adapters_paddle.py -x`
</verification>

<success_criteria>
- ADPT-01 对应链路可运行并满足统一出口/统一错误模型。
- D-30~D-35、D-38~D-42 在代码或测试中均有直接落点。
- 后续 GLM/MinerU 接入不需要重写 CLI 编排。
</success_criteria>

<output>
After completion, create `.planning/phases/03-adapters/03-01-SUMMARY.md`
</output>
