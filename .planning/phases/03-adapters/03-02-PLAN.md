---
phase: 03-adapters
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/eval_corpus/adapters/glm.py
  - src/eval_corpus/adapters/registry.py
  - src/eval_corpus/adapter_runner.py
  - src/eval_corpus/cli.py
  - tests/test_adapters_glm.py
  - tests/test_adapters_contract.py
autonomous: true
requirements:
  - ADPT-02
must_haves:
  truths:
    - "GLM-OCR 对同一夹具可输出合法 ParsedBlock[] 或可定位标准化错误。"
    - "GLM 鉴权/网络/解析失败在 continue-on-error 与 fail-fast 下行为可预测。"
    - "运行级元数据记录包含 tool_name/tool_version/model_id。"
  artifacts:
    - path: "src/eval_corpus/adapters/glm.py"
      provides: "GLM 统一适配器及错误阶段映射"
    - path: "tests/test_adapters_glm.py"
      provides: "GLM 成功与失败路径自动化测试"
    - path: "src/eval_corpus/adapter_runner.py"
      provides: "运行级元数据汇总输出"
  key_links:
    - from: "src/eval_corpus/adapters/glm.py"
      to: "src/eval_corpus/adapters/base.py"
      via: "统一协议实现 parse_to_blocks"
      pattern: "parse_to_blocks"
    - from: "src/eval_corpus/adapter_runner.py"
      to: "src/eval_corpus/cli.py"
      via: "run-level metadata 输出与 CLI 汇总展示"
      pattern: "tool_version|model_id"
---

<objective>
在既有统一契约上接入 GLM-OCR，确保云端链路在成功与失败场景都满足统一 IR 出口与可观测标准。

Purpose: 完整落实 D-36~D-40（版本/模型追踪与失败可观测）并覆盖 ADPT-02。  
Output: GLM adapter + 运行级元数据写入 + GLM 失败策略自动化验证。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapters/03-CONTEXT.md
@.planning/phases/03-adapters/03-RESEARCH.md
@src/eval_corpus/adapters/base.py
@src/eval_corpus/adapter_runner.py
@src/eval_corpus/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 GLM 适配器与最低一致语义映射（D-30, D-33, D-34, D-35）</name>
  <read_first>
    src/eval_corpus/adapters/base.py
    src/eval_corpus/adapters/paddle.py
    .planning/phases/03-adapters/03-CONTEXT.md
  </read_first>
  <files>
    src/eval_corpus/adapters/glm.py
    src/eval_corpus/adapters/registry.py
    tests/test_adapters_glm.py
  </files>
  <action>
    按 03-01 中已落地的统一协议接入 GLM：出口只能是 `list[ParsedBlock]`（per D-30/D-34），并强制最低一致语义（`type` 四值域、`text` 必填、`page` 可空、`heading_path` 默认 []，per D-33）。成功块必须写 `parser_tool="glm-ocr"`（per D-35）。注册到 registry，保证 CLI/runner 不感知 GLM 私有结构。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_glm.py::test_glm_normalizes_to_parsed_blocks -x</automated>
  </verify>
  <acceptance_criteria>
    GLM 成功样例可通过 ParsedBlock 校验；registry 能按工具名加载 GLM adapter；未出现 CLI 层工具私有分支。
  </acceptance_criteria>
  <done>GLM 适配器可被测试调用并输出统一 IR。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 完成 GLM 失败观测与运行级元数据记录（D-31, D-36, D-37, D-40）</name>
  <read_first>
    src/eval_corpus/adapter_runner.py
    src/eval_corpus/cli.py
    .planning/phases/03-adapters/03-RESEARCH.md
  </read_first>
  <files>
    src/eval_corpus/adapters/glm.py
    src/eval_corpus/adapter_runner.py
    tests/test_adapters_glm.py
    tests/test_adapters_contract.py
  </files>
  <action>
    将 GLM 的鉴权缺失、网络失败、响应结构异常分别映射到标准 `AdapterError`（per D-31）并保证错误最小字段齐全（file/tool/stage/message，debug 附 raw_error，per D-40）。在 runner 中强制记录运行级 `tool_name, tool_version, model_id`（per D-36），同时保持 block 级只存 `parser_tool` 不复制版本模型（per D-37）。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_glm.py::test_glm_error_envelope_and_run_metadata -x</automated>
  </verify>
  <acceptance_criteria>
    GLM 失败路径全部可定位到 stage+file；运行级元数据字段完整；block 级未引入 version/model 冗余字段。
  </acceptance_criteria>
  <done>GLM 的可观测性和可追溯性满足 D-31/D-36/D-37/D-40。</done>
</task>

<task type="auto">
  <name>Task 3: 绑定 GLM 到 CLI 策略矩阵（D-32, D-38, D-39）</name>
  <read_first>
    src/eval_corpus/cli.py
    src/eval_corpus/adapter_runner.py
    tests/test_adapters_contract.py
  </read_first>
  <files>
    src/eval_corpus/cli.py
    src/eval_corpus/adapter_runner.py
    tests/test_adapters_contract.py
  </files>
  <action>
    将 GLM 接入现有编排，不改动 D-32 边界：CLI 只调 runner + registry，不碰 GLM 原始响应。补充自动化用例：同批 3 文件中模拟 1 成功 2 失败，默认 continue-on-error 汇总全部结果（per D-38）；`--fail-fast` 在首错退出（per D-39）。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_contract.py::test_glm_in_runner_modes -x</automated>
  </verify>
  <acceptance_criteria>
    GLM 在两种执行策略下行为稳定且可预测，CLI 与 runner 输出与 03-01 契约一致。
  </acceptance_criteria>
  <done>ADPT-02 可执行闭环完成，不破坏已完成的 Paddle 行为。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process -> GLM API | 外部网络边界，鉴权与返回内容不受信 |
| GLM response -> adapter normalize | 第三方结构跨边界进入本地 IR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | S | `adapters/glm.py` | mitigate | 仅从环境变量读取 API key，禁止明文参数回显；请求头和日志做脱敏 |
| T-03-05 | I | `adapters/glm.py` | mitigate | debug 模式外不落原始响应和 key；raw_error 仅在 debug 且脱敏输出 |
| T-03-06 | T | `adapter_runner.py` | mitigate | 对 run metadata 字段固定白名单并测试校验，避免被外部响应污染 |
</threat_model>

<verification>
运行：
- `pytest -q tests/test_adapters_glm.py -x`
- `pytest -q tests/test_adapters_contract.py::test_glm_in_runner_modes -x`
</verification>

<success_criteria>
- ADPT-02 交付完成，GLM 与统一契约完全对齐。
- D-36/D-37 元数据策略在运行级落地且有测试证明。
- 继续执行与快速失败策略对 GLM 生效且不影响已接入工具。
</success_criteria>

<output>
After completion, create `.planning/phases/03-adapters/03-02-SUMMARY.md`
</output>
