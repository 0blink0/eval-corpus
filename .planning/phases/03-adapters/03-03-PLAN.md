---
phase: 03-adapters
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/eval_corpus/adapters/mineru.py
  - src/eval_corpus/adapters/registry.py
  - src/eval_corpus/adapter_runner.py
  - src/eval_corpus/cli.py
  - tests/fixtures/adapters/README.md
  - tests/test_adapters_mineru.py
  - tests/test_adapters_contract.py
autonomous: true
requirements:
  - ADPT-03
must_haves:
  truths:
    - "MinerU 链路可对同一夹具输出合法 ParsedBlock[] 或可定位标准化错误。"
    - "同一批三件套夹具上，三工具均满足 D-43 的验收定义。"
    - "CLI 最终批处理输出可同时体现 continue-on-error 与 fail-fast 行为。"
  artifacts:
    - path: "src/eval_corpus/adapters/mineru.py"
      provides: "MinerU 到 ParsedBlock[] 的统一映射实现"
    - path: "tests/test_adapters_mineru.py"
      provides: "MinerU 成功/失败路径测试"
    - path: "tests/test_adapters_contract.py"
      provides: "跨三工具的同批验收测试"
  key_links:
    - from: "src/eval_corpus/adapters/mineru.py"
      to: "src/eval_corpus/adapters/base.py"
      via: "统一协议与错误模型复用"
      pattern: "AdapterError|parse_to_blocks"
    - from: "tests/test_adapters_contract.py"
      to: "tests/fixtures/adapters/"
      via: "三件套样例统一驱动三工具"
      pattern: "text|scan|table"
---

<objective>
完成第三条工具链（MinerU）并执行跨三工具同批夹具验收，确保 Phase 3 的 ADPT-01~03 和 D-30~D-43 全量闭环。

Purpose: 在不简化任何锁定决策前提下完成最终验收口径，把 Phase 3 输出打磨为可直接供 Phase 4 消费的统一输入。  
Output: MinerU adapter + 三工具同批自动化验收 + 完整可观测批处理结果。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapters/03-CONTEXT.md
@.planning/phases/03-adapters/03-RESEARCH.md
@src/eval_corpus/adapters/base.py
@src/eval_corpus/adapter_runner.py
@tests/fixtures/adapters/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 MinerU 适配器与标准错误阶段映射（D-30, D-31, D-34, D-35）</name>
  <read_first>
    src/eval_corpus/adapters/base.py
    src/eval_corpus/adapters/glm.py
    .planning/phases/03-adapters/03-RESEARCH.md
  </read_first>
  <files>
    src/eval_corpus/adapters/mineru.py
    src/eval_corpus/adapters/registry.py
    tests/test_adapters_mineru.py
  </files>
  <action>
    实现 MinerU adapter，出口严格为 `list[ParsedBlock]`（per D-30/D-34），并写入 `parser_tool="mineru"`（per D-35）。将 CLI 调用失败/输出异常/归一化失败映射到 `load|parse|normalize|validate` 标准阶段错误（per D-31），字段保持统一错误包结构，保证与 Paddle/GLM 行为一致。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_mineru.py::test_mineru_normalize_and_error_stage_mapping -x</automated>
  </verify>
  <acceptance_criteria>
    MinerU 成功/失败都能走统一接口；错误 stage 可定位且字段完整；registry 注册后可被 runner 加载。
  </acceptance_criteria>
  <done>MinerU 适配器单体测试通过，三工具均已实现统一协议。</done>
</task>

<task type="auto">
  <name>Task 2: 收口运行级元数据与执行策略一致性（D-36, D-37, D-38, D-39, D-40）</name>
  <read_first>
    src/eval_corpus/adapter_runner.py
    src/eval_corpus/cli.py
    tests/test_adapters_contract.py
  </read_first>
  <files>
    src/eval_corpus/adapter_runner.py
    src/eval_corpus/cli.py
    tests/test_adapters_contract.py
  </files>
  <action>
    在三工具共跑场景下校验并补齐 runner：每个工具都写入运行级 `tool_name/tool_version/model_id`（per D-36），block 级仅保留 `parser_tool`（per D-37）；默认 continue-on-error 不中断批次并汇总失败（per D-38），`--fail-fast` 首错退出（per D-39）；所有错误明细至少含 file/tool/stage/message，debug 才输出 raw_error（per D-40）。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_contract.py::test_three_tool_runtime_metadata_and_modes -x</automated>
  </verify>
  <acceptance_criteria>
    三工具运行级元数据完整一致；两种执行模式行为与规范一致；错误清单字段完整且可机器解析。
  </acceptance_criteria>
  <done>三工具在统一 runner/CLI 下具备一致可观测性与可追溯性。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 执行最小三件套跨工具验收（D-41, D-42, D-43）</name>
  <read_first>
    tests/fixtures/adapters/README.md
    tests/test_adapters_paddle.py
    tests/test_adapters_glm.py
    tests/test_adapters_mineru.py
  </read_first>
  <files>
    tests/fixtures/adapters/README.md
    tests/test_adapters_contract.py
  </files>
  <action>
    固化并执行同一批最小三件套夹具（文本 PDF/扫描 PDF/表格样例，per D-41），夹具目录保持 `tests/fixtures/adapters/`（per D-42）。新增跨工具验收测试：对每个工具与每个样例断言“要么返回合法 ParsedBlock[]，要么返回可定位标准化错误”，以此作为 D-43 的自动化验收标准。
  </action>
  <verify>
    <automated>pytest -q tests/test_adapters_contract.py::test_cross_tool_minimal_fixture_acceptance -x</automated>
  </verify>
  <acceptance_criteria>
    同批三件套上三工具全部满足 D-43 定义；任一失败都可定位文件与阶段；README 与测试对夹具契约描述一致。
  </acceptance_criteria>
  <done>ADPT-03 完成并形成 Phase 3 全量验收闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file -> MinerU process | 非受信文件交给外部解析进程 |
| multi-adapter results -> aggregate report | 多来源结果合并时可能发生混淆/丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | D | `adapters/mineru.py` | mitigate | 子进程调用设置超时与退出码检查，异常统一落 stage 错误防止批处理失控 |
| T-03-08 | R | `adapter_runner.py` | mitigate | 结果汇总包含 file+tool+stage+message 与 run metadata，确保失败可追责可复现 |
| T-03-09 | T | `tests/test_adapters_contract.py` | mitigate | 固化同批夹具契约与跨工具验收断言，防止映射语义漂移未被发现 |
</threat_model>

<verification>
运行：
- `pytest -q tests/test_adapters_mineru.py -x`
- `pytest -q tests/test_adapters_contract.py::test_three_tool_runtime_metadata_and_modes -x`
- `pytest -q tests/test_adapters_contract.py::test_cross_tool_minimal_fixture_acceptance -x`
</verification>

<success_criteria>
- ADPT-03 完成，三工具链路全部可运行。
- D-41~D-43 的夹具与验收口径自动化落地。
- Phase 3 产物可直接作为 Phase 4 指标输入且错误定位信息完备。
</success_criteria>

<output>
After completion, create `.planning/phases/03-adapters/03-03-SUMMARY.md`
</output>
