---
phase: 04-metrics
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/eval_corpus/metrics/semantic_review.py
  - src/eval_corpus/metrics/calculators.py
  - tests/test_metrics_semantic.py
autonomous: true
requirements:
  - METR-07
must_haves:
  truths:
    - "语义完整率采用规则主导自动评分，并可重复执行（D-46）"
    - "自动评分与人工抽检记录分开存储，且人工结果不覆盖自动分（D-47）"
    - "语义指标同样遵循 not_applicable 与 errors 分流（D-45, D-52, D-53）"
  artifacts:
    - path: src/eval_corpus/metrics/semantic_review.py
      provides: 固定抽样与人工复核登记接口
    - path: src/eval_corpus/metrics/calculators.py
      provides: METR-07 自动规则评分函数
    - path: tests/test_metrics_semantic.py
      provides: METR-07 可复现性与分离存档测试
  key_links:
    - from: src/eval_corpus/metrics/semantic_review.py
      to: src/eval_corpus/metrics/calculators.py
      via: 采样结果驱动语义评分
      pattern: "semantic|sample|review"
    - from: tests/test_metrics_semantic.py
      to: src/eval_corpus/metrics/semantic_review.py
      via: 固定种子采样一致性断言
      pattern: "seed|deterministic"
---

<objective>
完成 METR-07：规则自动评分 + 固定抽样人工复核登记，确保可复现且不混淆两类结果。

Purpose: 在不引入 LLM-as-judge 强依赖前提下，先交付可审计的语义完整率主路径。  
Output: METR-07 计算函数、抽检登记模块、语义指标测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics/04-CONTEXT.md
@.planning/phases/04-metrics/04-RESEARCH.md
@src/eval_corpus/metrics/models.py
@src/eval_corpus/metrics/calculators.py

<interfaces>
From src/eval_corpus/metrics/models.py (from 04-01):
- MetricResult(raw_value, numerator, denominator, excluded_count, applicable_count, total_count, level, threshold)

From decisions D-46/D-47:
- 自动评分与人工抽检必须并存且分离字段，不允许覆盖写入同一分值字段。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 METR-07 可复现抽检与分离存档测试</name>
  <read_first>.planning/phases/04-metrics/04-CONTEXT.md, src/eval_corpus/metrics/models.py, tests</read_first>
  <files>tests/test_metrics_semantic.py</files>
  <acceptance_criteria>测试中包含 test_semantic_auto_score_deterministic、test_manual_review_separate_from_auto_score、test_semantic_not_applicable_handling 三个用例名；固定 seed 下样本索引断言一致。</acceptance_criteria>
  <action>先写 METR-07 行为测试：规则自动分稳定可复算，人工抽检记录仅落审计字段，不覆盖 auto_score；缺失文本或不可判定样本进入 not_applicable。</action>
  <verify>
    <automated>pytest tests/test_metrics_semantic.py -q -x</automated>
  </verify>
  <done>METR-07 的核心边界行为被测试锁定，后续实现可直接对齐。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现固定抽样与人工复核登记接口</name>
  <read_first>.planning/phases/04-metrics/04-RESEARCH.md, src/eval_corpus/metrics/models.py, tests/test_metrics_semantic.py</read_first>
  <files>src/eval_corpus/metrics/semantic_review.py</files>
  <acceptance_criteria>semantic_review.py 中存在 deterministic sampling 入口（含 seed 参数）与 manual review 记录结构；可 grep 到 auto_score 与 manual_review 两组独立字段名。</acceptance_criteria>
  <action>实现固定采样函数（输入样本集合 + seed + sample_size，输出稳定索引）；实现人工复核登记结构（reviewer、sample_id、note、manual_label），并保持与自动评分字段分离（D-47）。</action>
  <verify>
    <automated>python -c "from eval_corpus.metrics.semantic_review import select_semantic_samples; print('ok')"</automated>
  </verify>
  <done>语义抽检接口可复现且可审计，满足 D-46/D-47。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 在 calculators 中接入 METR-07 自动评分</name>
  <read_first>src/eval_corpus/metrics/calculators.py, src/eval_corpus/metrics/semantic_review.py, tests/test_metrics_semantic.py</read_first>
  <files>src/eval_corpus/metrics/calculators.py, tests/test_metrics_semantic.py</files>
  <acceptance_criteria>calculators.py 暴露 compute_metric_07_semantic_completeness；返回 MetricResult 且包含 applicable_count/total_count；pytest tests/test_metrics_semantic.py 全绿。</acceptance_criteria>
  <action>新增 METR-07 规则评分函数，规则可配置但默认固定；输出 raw_value、阈值、等级与 not_applicable 统计，严格按 D-45/D-52/D-53 处理异常样本，不引入 LLM 并行评分通道。</action>
  <verify>
    <automated>pytest tests/test_metrics_semantic.py -q -x</automated>
  </verify>
  <done>METR-07 与 METR-01~06 同构，成为可聚合的标准指标结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic sample selection -> manual review ledger | 抽样规则与人工记录都可能被误用导致不可复现 |
| calculator output -> downstream aggregation | 语义结果字段缺失会破坏汇总一致性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | R | src/eval_corpus/metrics/semantic_review.py | mitigate | 固定 seed 与样本索引落盘，确保同输入重复运行可复现 |
| T-04-05 | T | src/eval_corpus/metrics/calculators.py | mitigate | 自动评分与人工复核字段分离，禁止覆盖自动 raw_value |
| T-04-06 | I | tests/test_metrics_semantic.py | accept | 不含敏感信息，仅包含测试样本与规则标签，低风险 |
</threat_model>

<verification>
执行 `pytest tests/test_metrics_semantic.py -q -x`。  
再执行 `pytest tests/test_metrics_core.py tests/test_metrics_semantic.py -q` 验证指标模块无回归。
</verification>

<success_criteria>
METR-07 可复现、可抽检、可审计，并与既有指标模型保持统一返回契约。
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics/04-02-SUMMARY.md`
</output>
