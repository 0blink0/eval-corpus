---
phase: 05-reporting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eval_corpus/synthetic_data/models.py
  - src/eval_corpus/synthetic_data/generator.py
  - src/eval_corpus/batch/models.py
  - src/eval_corpus/batch/runner.py
  - tests/test_synthetic_data.py
  - tests/test_batch_runner.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "无真料场景可生成文本型/扫描型/表格型样本并可复现（D-60, D-61）"
    - "批跑默认 continue-on-error，失败文件会汇总输出（D-64）"
    - "可配置并行度、失败阈值、重试次数，且不引入分布式队列（D-59, D-65）"
  artifacts:
    - path: "src/eval_corpus/synthetic_data/generator.py"
      provides: "三类样本参数化生成"
    - path: "src/eval_corpus/batch/runner.py"
      provides: "目录递归批处理与失败重试策略"
    - path: "tests/test_batch_runner.py"
      provides: "continue-on-error/阈值/重试契约测试"
  key_links:
    - from: "src/eval_corpus/synthetic_data/generator.py"
      to: "src/eval_corpus/batch/runner.py"
      via: "生成样本目录直接作为批跑输入"
      pattern: "input_dir|output_dir|seed"
    - from: "src/eval_corpus/batch/runner.py"
      to: "src/eval_corpus/adapter_runner.py"
      via: "单文件执行结果聚合为批次报告"
      pattern: "run_adapter_on_files|errors|results"
---

<objective>
交付可复现的合成数据生成器与单机批跑编排能力，满足 CI smoke 与目录扩跑。

Purpose: 满足 DATA-01 与 DATA-02，同时落实 continue-on-error 与失败控制策略（D-59, D-64, D-65）。
Output: 生成器模块、批跑模块、对应自动化测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reporting/05-CONTEXT.md
@.planning/phases/05-reporting/05-RESEARCH.md
@src/eval_corpus/adapter_runner.py
@src/eval_corpus/scan.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现三类型合成数据生成器（可复现）</name>
  <files>src/eval_corpus/synthetic_data/models.py, src/eval_corpus/synthetic_data/generator.py, tests/test_synthetic_data.py</files>
  <behavior>
    - Test 1: 生成结果覆盖 text/scan/table 三类样本（D-60）。
    - Test 2: 相同 seed 与参数重复运行得到一致文件清单与内容摘要（D-61）。
  </behavior>
  <action>新增生成参数模型（数量、长度、类型比例、seed）并实现样本输出；扫描型与表格型样本至少包含可识别结构标记，便于后续流水线处理。禁止将样本写死为固定单文件，必须支持规模参数（per D-61）。</action>
  <verify>
    <automated>pytest tests/test_synthetic_data.py -q</automated>
  </verify>
  <done>合成数据可按参数生成三类型样本，且可复现测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现目录递归批跑与失败策略控制</name>
  <files>src/eval_corpus/batch/models.py, src/eval_corpus/batch/runner.py, tests/test_batch_runner.py</files>
  <behavior>
    - Test 1: 默认 continue-on-error 下，单文件失败不终止整批并产出失败清单（D-64）。
    - Test 2: 失败率超过阈值时中止批跑，返回非成功状态（D-65）。
    - Test 3: 设置重试次数时，失败文件按配置重试并记录最终状态（D-65）。
  </behavior>
  <action>实现 batch 配置与执行器：目录递归收集输入、并行度参数、失败阈值、重试开关与次数。严格限定单机执行模型，不新增分布式队列或外部调度依赖（per D-59）。输出批次级统计（总量、成功、失败、重试后成功、终止原因）。</action>
  <verify>
    <automated>pytest tests/test_batch_runner.py -q</automated>
  </verify>
  <done>批跑器具备目录递归、并行控制、失败阈值与重试能力，测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/runtime parameters -> synthetic generator | 外部参数决定生成规模与内容，可能导致资源滥用 |
| input directory -> batch runner | 不可信路径与文件进入批跑流程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | D | `synthetic_data/generator.py` | mitigate | 限制生成规模上限并校验参数范围，防止资源耗尽 |
| T-05-05 | T | `batch/runner.py` | mitigate | 对输入/输出目录做 `resolve` 与根路径约束，拒绝越界 |
| T-05-06 | E | `batch/runner.py` | accept | 本地 CLI 无多租户权限边界，风险低；保留日志审计 |
</threat_model>

<verification>
运行 `pytest tests/test_synthetic_data.py tests/test_batch_runner.py -q`，并验证同 seed 结果稳定、失败阈值行为符合预期。
</verification>

<success_criteria>
DATA-01/DATA-02 完成：可复现合成数据可用于 smoke，目录批跑可在单机参数化扩跑并可控处理失败。
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting/05-02-SUMMARY.md`
</output>
